/**
 * Здесь описываем порядок конкатенации файлов.
 * Если пустой массив, то сортировка файлов будет по умолчанию в алфавитном порядке.
 * Можно указать только один файл, который будет идти первым,
 * а остальные не указанные файлы будут в алфавитном порядке.
 * Также можно указать несколько файлов, которые будут первыми.
 * В данный момент поддерживает склеивание файлов *.js и *.css
 */
module.exports = {
	js: [],

	css: [],
};